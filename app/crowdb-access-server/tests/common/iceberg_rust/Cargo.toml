[package]
name = "crowdb-iceberg-rust-client-fixture"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
iceberg = "=0.10.0"
iceberg-catalog-rest = "=0.10.0"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
